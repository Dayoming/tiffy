<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tiffy.repository.UserMapper">

    <!-- 데이터 삽입 -->
    <insert id="insertUser" parameterType="User">
        INSERT INTO `user` (username, password, email, nickname)
        VALUES (#{username}, #{password}, #{email}, #{nickname})
    </insert>

    <!-- ID로 사용자 찾기 -->
    <select id="findUserById" parameterType="Long" resultType="User">
        SELECT * FROM `user` WHERE id = #{id}
    </select>

    <!-- 사용자 닉네임으로 사용자 찾기 - 키워드 포함 -->
    <select id="findUserByIncludeUserNickname" parameterType="String" resultType="User">
        SELECT * FROM `user` WHERE nickname LIKE CONCAT('%', #{nickname}, '%')
    </select>

    <!-- 사용자 닉네임으로 사용자 찾기 - 완벽히 일치 -->
    <select id="findUserByUserNickname" parameterType="String" resultType="User">
        SELECT * FROM `user` WHERE nickname = #{nickname}
    </select>

    <!-- 사용자 아이디로 사용자 찾기 - 완벽히 일치 -->
    <select id="findUserByUsername" parameterType="String" resultType="User">
        SELECT * FROM `user` WHERE username = #{username}
    </select>

    <!-- 데이터 수정 -->
    <update id="updateUser" parameterType="User">
        UPDATE `user` SET
        username = #{username},
        password = #{password},
        email = #{email},
        nickname = #{nickname}
        WHERE id = #{id}
    </update>

    <!-- 장소 업데이트 -->
    <update id="updateUserPlace" parameterType="User">
        UPDATE `user` SET
        place = #{place}
        WHERE id = #{id}
    </update>

    <!-- 데이터 삭제 -->
    <delete id="deleteUserById" parameterType="Long">
        DELETE FROM `user` WHERE id = #{id}
    </delete>

</mapper>
