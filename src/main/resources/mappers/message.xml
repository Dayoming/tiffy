<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tiffy.repository.MessageMapper">
    <!-- 채팅방 내 메시지 조회 -->
    <select id="getMessagesByChatRoomId" parameterType="Long" resultType="Message">
        SELECT * FROM Message WHERE chatroom_id = #{chatRoomId} ORDER BY timestamp
    </select>

    <!-- 메시지 저장 -->
    <insert id="saveMessage" parameterType="Message">
        INSERT INTO Message (chatroom_id, sender_id, receiver_id, content) VALUES (#{chatRoomId}, #{senderId}, #{receiverId}, #{content})
    </insert>
</mapper>
